`make eval` 的结果快照落在这里，`make eval-diff` 拿最近两份做对比。

**不入库**（见根目录 `.gitignore`）：每份几十 KB 的完整模型输出，
每台机器、每次跑都不一样，进版本库只会让每个 diff 都变成噪音。

要留档的是**结论**，写进 `specs/006-m6-tuning/tasks.md` —— 
阈值定成多少、依据是哪条曲线、改动前后哪几道题变了。
