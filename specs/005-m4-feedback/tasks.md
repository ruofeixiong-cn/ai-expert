# M4 · 反馈与看板 —— 任务清单（Tasks）

> `[x]` = 已验证通过　`[~]` = 代码已写但未跑过　`[ ]` = 未做

## V1 契约冻结
- [x] V1.1 `POST /api/chat/{slug}/feedback`
- [x] V1.2 `GET /api/experts/{id}/stats`
- [x] V1.3 `GET /api/chat/{slug}` 补 `history`（补 M3 漏洞）
- [x] V1.4 `make contract` + 三方 typecheck

## V2 数据库（迁移 0011）
- [x] V2.1 `feedbacks` 表 + 唯一索引 (message_id, fan_user_id)
- [x] V2.2 RLS 三件套
- [x] V2.3 **不给** `app_agent` 任何权限（允许清单）
- [x] V2.4 Python 镜像 + 漂移测试

## V3 backend 服务
- [x] V3.1 `submitFeedback`：三道校验 + upsert
- [x] V3.2 `getStats`：四个数
- [x] V3.3 `listBlindspots`：LATERAL 取问题原文
- [x] V3.4 `getChatExpert` 带 history + myRating

## V4 backend 路由与测试
- [x] V4.1 两个路由接上
- [x] V4.2 F2 改主意（唯一索引 upsert）
- [x] V4.3 **F4 打不了别人的分** → 404
- [x] V4.4 **F5 跨租户** → 404
- [x] V4.5 F6 / F7 / F8 盲区判定三种情形
- [x] V4.6 F9 / F10 看板口径
- [x] V4.7 F12 未登录 / 非本人 → 401 / 404

## V5 前端
- [x] V5.1 分享页赞/踩 + 可选原因
- [x] V5.2 从 `meta` 事件取 `message_id`
- [x] V5.3 历史消息渲染 + 已有评价回显（F1）
- [x] V5.4 专家详情页看板卡片（收入不粉饰、满意度 null 不显示 0%）
- [x] V5.5 盲区列表（F11 问题原文）
- [x] V5.6 `tsc` + `vite build`

## V6 端到端
- [x] V6.1 提问 → 点踩 → 看板盲区数 +1
- [x] V6.2 问库里没有的 → 无需点击即计入盲区（F8）
- [x] V6.3 `make verify` 全绿

---

## 当前状态：M4 完成 ✅

```
make verify → backend 64 + agent 56 + 端到端 15（2 条真实模型的跳过）
```

### 验收结果

| # | 验收 | 结果 |
|---|---|---|
| F1 | 刷新后按钮保持选中 | ✅ 浏览器实测：整页重载后历史和踩的状态都在 |
| F2 | 改主意 | ✅ 唯一索引 upsert，一条记录，旧原因被清掉 |
| F3 | 可选原因 ≤200 字 | ✅ 超长返回 1001 |
| F4 | **打不了别人的分** | ✅ 404，库里无新增 |
| F5 | **跨租户** | ✅ 404 |
| F6 | 低置信度 → 盲区，无需点击 | ✅ **规则被真实模型推翻重写过一次，见 spec §5** |
| F7 | 高置信度 + 点踩 ≠ 盲区 | ✅ 进满意度，不进盲区 |
| F8 | `no_context` 无需点击即计入 | ✅ |
| F9 | 四个数 | ✅ |
| F10 | 无反馈时满意度为 `null` | ✅ 前端显示「暂无评价」 |
| F11 | 盲区列表给问题原文 | ✅ 浏览器实测：「港股打新要准备多少现金 / 相关内容太少」 |
| F12 | 未登录 401 / 非本人 404 | ✅ |

### 实测数据（真实 rerank，同一个专家同一批切片）

```
「定投的手续费怎么算」      0.8375  库里讲过    → 不是盲区
「港股打新要准备多少现金」  0.1163  库里没讲过  → 盲区
```

### 这一步找出来的四件事

| # | 是什么 | 怎么发现的 | 为什么单测发现不了 |
|---|---|---|---|
| 1 | 🔴 盲区规则漏掉最典型的盲区 | 真实模型手动试第一次就漏 | 假 rerank 按字符重合打分，造不出「擦着闸门过去」这种状态 |
| 2 | 🔴 `meta.message_id` 指向空气 | 端到端反馈接口稳定 404 | 三方各造各的 id，两边单测都自洽 |
| 3 | 🔴 `created_at` 排不出会话内顺序 | 写测试时种子数据用了一个事务 | 生产上两条消息分属两个事务，一直「看起来正常」 |
| 4 | 🟠 端到端跑在旧代码上 | 改了 agent 但行为没变 | 残留进程占着端口，健康检查被它答应了 |

第 1 条是这一步最有价值的产出：**按文档写完、三层测试全绿、
接上真实模型第一次就漏。** 详细过程留在 spec §5，没有把错的那版删掉。

### 下一步

M5 付费：credits 账本、兑换码、微信支付。
看板的「收入」那一格现在恒为 0，等它填。

⚠️ **域名 ICP 备案是纯等待期（2~3 周），卡在微信支付前面 —— 现在就该去办。**
