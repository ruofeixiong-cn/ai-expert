node_modules/
dist/

# Claude Code：skills 与 launch.json 入库（团队共享），
# 但 settings.local.json 是每个人自己的权限授权记录，不该推给别人。
.claude/settings.local.json
.venv/
__pycache__/
*.pyc
.env
.env.local
.pnpm-store/
.vite/
coverage/
.DS_Store
*.log

# 第三方版权材料：本地参考用，不入库
docs/reference/*.pdf

# 评测快照：每次跑分的完整模型输出，几十 KB 起，且每台机器都不一样。
# --diff 只需要本地有两份；真正要留档的结论写在 specs/006-m6-tuning/tasks.md。
agent/eval/snapshots/*.json
